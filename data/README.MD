# Data

This repository includes a small sample dataset (`sample.csv`) for testing
and demonstration.

The full dataset is not tracked due to size.
To use the full dataset:
1. Download it from <link or source>
2. Place it in the `data/` directory
3. Run `data.py`
